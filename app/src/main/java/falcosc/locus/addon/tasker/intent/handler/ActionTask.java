package falcosc.locus.addon.tasker.intent.handler;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.os.Bundle;
import android.support.annotation.NonNull;

public class ActionTask implements TaskerAction {
    @Override
    public void handle(@NonNull Context context, Intent intent, @NonNull Bundle bundle, @NonNull BroadcastReceiver receiver) {

    }
}
